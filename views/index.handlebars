<h1>Burgers</h1>

<h2>Burgers ready to eat</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured = 1}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers you can order</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured = 0}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Cat</h2>
<form class="create-form">

  <div class="form-group">
    <label for="bu">What burger would you like to order?</label>
    <input type="text" id="burgerId" name="burger_name">
  </div>

  <button type="submit">Order up!</button>
</form>
